import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  // styles here
  tabMenu: {
    backgroundColor: "#fff"
  },
  selectedTabTitle: {
    color: "white"
  },
  unselectedTabTitle: {
    color: "#999"
  }
});

export default styles;
